import SwiftUI
import DesignSystem
import CoreKit

struct BirthdaysScreen: View {
    var body: some View {
        DSLabel("Birthdays")
            .padding()
            .frame(width: 200.fitW)
    }
}
